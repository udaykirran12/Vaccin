<!DOCTYPE html>
<html>
<head>
  <style>
    /* Classic CSS Styles */
    body {
      font-family: Arial, sans-serif;
    }

    h1 {
      color: #fff;
      font-size: 24px;
      text-align: center;
      background-color: #007bff;
      padding: 10px;
      margin-top: 0;
      position: relative;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    .button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
      margin: 5px; /* Add margin to buttons */
    }

    .button:hover {
      background-color: #0056b3;
    }

    .add-button {
      position: absolute;
      top: 50%;
      right: 20px;
      transform: translateY(-50%);
      background-color: white;
      color: black;
      cursor: pointer;
    }

    .form-row {
      margin-bottom: 20px;
      display: flex;
      align-items: center;
    }

    .form-row input[type="text"] {
      padding: 10px;
      border: 1px solid #007bff;
      border-radius: 4px;
      width: 200px;
    }

    .form-row button {
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-left: 10px;
    }

    .form-row button:hover {
      background-color: #0056b3;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ccc;
    }

    /* Additional Styles */
    .one, .two {
      margin: 10px; /* Add margin to class=one and class=two divs */
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Vaccination Centres</h1> <!-- Replace "Vaccination Centre" with "CoviNOW" -->
    <div class="form-row">
      <div class="one">
        <input type="text" placeholder="Search by District">
        <button class="button">Search</button>
        <form action="/handleCentre" method="post">
          <input type="text" placeholder="Enter Hosp ID" name="id">
          <button type="submit" class="button">Delete</button>
        </form>
      </div>
      <div class="two">
        <form action="/handleCentre" method="get">
          <button type="submit" class="button">Add Vaccine Center</button>
        </form>
        <form action="/adminLogin/adminDashboard/updateCentre" method="get">
          <button type="submit" class="button">Update</button>
        </form>
      </div>
    </div>
    <table>
      <tr>
          <th>Hosp. ID</th>
          <th>Hosp. Name</th>
          <th>Address</th>
          <th>No. of doses booked today</th>
      </tr>
      <% for (var i = 0; i < list.length; i++) { %>
          <% if ((list[i].name).length === 0 || (list[i].address).length === 0) { %>
              <% continue; %>
          <% } %>
          <tr>
              <td><%=list[i].id%></td>
              <td><%= list[i].name %></td>
              <td><%= list[i].address %></td>
              <td><%=list[i].doses%></td>
          </tr>
      <% } %>
    </table>
  </div>
</body>
</html>
